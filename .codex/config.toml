# Project-level Codex configuration for survey workflows.
# Adjust sandbox/approval settings in the Codex app as needed.

web_search = "live"

[agents]
max_threads = 8
max_depth = 1
job_max_runtime_seconds = 2400